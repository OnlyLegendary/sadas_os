[package]
name = "sadas-kernel"
version = "0.1.0"
edition.workspace = true

[dependencies]
sadas-platform = { path = "../platform" }
sadas-sysapi = { path = "../sysapi" }
sadas-logging = { path = "../logging" }
sadas-boot-protocol = { path = "../boot-protocol" }
sadas-console = { path = "../console" }
sadas-memory = { path = "../memory" }
sadas-arch-x86_64 = { path = "../arch-x86_64" }
sadas-syscall = { path = "../syscall" }
sadas-exec = { path = "../exec" }